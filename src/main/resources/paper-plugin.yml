name: dynamic-areas
version: '$version'

main: dev.necr0manthre.dynamicareas.DynamicAreas
api-version: '26.2'
load: POSTWORLD
bootstrapper: dev.necr0manthre.dynamicareas.DynamicAreaBootstrap
dependencies:
  server:
    WorldEdit:
      load: BEFORE
      required: false

permissions:
  dynamicareas.use:
    default: op
